<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground"
    android:fitsSystemWindows="true">

    <!-- Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_height"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/colorToolbar"
        android:elevation="4dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp">

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/master_password_title_create"
            android:textSize="@dimen/toolbar_title_size"
            android:textColor="@color/colorToolbarText"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Scrollable Content -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:padding="32dp">

            <!-- Lock Icon -->
            <ImageView
                android:id="@+id/icon_lock"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_marginTop="32dp"
                android:src="@drawable/ic_lock"
                android:contentDescription="@string/lock"
                android:tint="@color/colorPrimary" />

            <!-- Subtitle -->
            <TextView
                android:id="@+id/text_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/master_password_subtitle_create"
                android:textSize="@dimen/text_size_sm"
                android:textColor="@color/colorTextSecondary"
                android:gravity="center" />

            <!-- Password Field -->
            <EditText
                android:id="@+id/edit_password"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:hint="@string/master_password_hint"
                android:inputType="textPassword"
                android:background="@drawable/bg_password_input"
                android:textSize="@dimen/text_size_md"
                android:textColor="@color/colorTextPrimary"
                android:textColorHint="@color/colorTextHint"
                android:singleLine="true" />

            <!-- Confirm Password Field (visible only in CREATE mode) -->
            <EditText
                android:id="@+id/edit_confirm_password"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:hint="@string/master_password_confirm_hint"
                android:inputType="textPassword"
                android:background="@drawable/bg_password_input"
                android:textSize="@dimen/text_size_md"
                android:textColor="@color/colorTextPrimary"
                android:textColorHint="@color/colorTextHint"
                android:singleLine="true"
                android:visibility="gone" />

            <!-- Error Message -->
            <TextView
                android:id="@+id/text_error"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textSize="@dimen/text_size_sm"
                android:textColor="@color/colorTrash"
                android:visibility="gone" />

            <!-- Password Strength Hint (visible only in CREATE mode) -->
            <TextView
                android:id="@+id/text_strength_hint"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@string/master_password_strength_hint"
                android:textSize="@dimen/text_size_xs"
                android:textColor="@color/colorTextSecondary"
                android:visibility="gone" />

            <!-- Action Button -->
            <Button
                android:id="@+id/btn_action"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="24dp"
                android:text="@string/master_password_btn_create"
                android:textColor="@color/colorTextOnPrimary"
                android:textSize="@dimen/text_size_md"
                android:textStyle="bold"
                android:background="@drawable/bg_password_button" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
